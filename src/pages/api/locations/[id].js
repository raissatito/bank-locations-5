export default async function handler(req, res) {
  if (req.method == 'PUT') {
    // TODO
  }

  if (req.method == 'DELETE') {
    // TODO
  }

  if (req.method == 'GET') {
    // TODO
  }
  res.status(450).json({error: true})
}